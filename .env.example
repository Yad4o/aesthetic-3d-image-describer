# Blink Project Credentials
# These are automatically handled by the platform in the sandbox,
# but required for local development or manual deployment.

NEXT_PUBLIC_BLINK_PROJECT_ID=your-project-id
NEXT_PUBLIC_BLINK_PUBLISHABLE_KEY=your-publishable-key
BLINK_SECRET_KEY=your-secret-key

# AI Configuration (Optional if using Blink AI wrapper)
# GEMINI_API_KEY=your-gemini-key
